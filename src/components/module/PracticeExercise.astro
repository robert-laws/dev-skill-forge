---
interface Props {
  prompt?: string;
}

const { prompt } = Astro.props;
---

{
  prompt && (
    <section class="section-block panel">
      <h2 class="section-title compact">Practice Exercise</h2>
      <p>{prompt}</p>
    </section>
  )
}
